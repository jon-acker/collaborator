module.exports = function(grunt) {
    'use strict';

	grunt.initConfig({
		gruntSpec: {
			src: 'src/',
			spec: 'spec/'
		}
	});

    grunt.loadTasks('tasks/');
	grunt.loadNpmTasks('grunt-spec');
};
